setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: app/Dockerfile
release:
  image: web
  command:
    - ./prod-release.sh
run:
  web: uwsgi --ini /code/app/uwsgi.ini -w app.wsgi
